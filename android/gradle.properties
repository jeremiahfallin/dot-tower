org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# Overwritten by scripts/android-setup.sh with whatever NDK it actually
# installed. Gradle needs this to resolve the games-activity prefab.
ndkVersion=30.0.15729638
